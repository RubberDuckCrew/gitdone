name: Close Actions

on:
  issues:
    types: [closed]
  pull_request:
    types: [closed]

jobs:
  run:
    uses: RubberDuckCrew/.github/.github/workflows/action-close-actions.yml@ce18923b2f09dce39930cd8c370ea45c8e58c414 # v2.0.0
    permissions:
      issues: write
      pull-requests: write
      actions: write

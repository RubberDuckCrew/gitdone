name: Sync labels

on:
  schedule:
    - cron: "0 3 * * 0"
  workflow_dispatch:

jobs:
  run:
    uses: RubberDuckCrew/.github/.github/workflows/action-sync-labels.yml@ce18923b2f09dce39930cd8c370ea45c8e58c414 # v2.0.0
    permissions:
      issues: write
    with:
      additional-config: ".github/labels.yml"

name: Sync labels

on:
  schedule:
    - cron: "0 3 * * 0"
  workflow_dispatch:

jobs:
  run:
    uses: RubberDuckCrew/.github/.github/workflows/action-sync-labels.yml@0968afd074c1d7ee5654414dc45027fc9c35d8d3 # v1
    permissions:
      issues: write
    with:
      additional-config: ".github/labels.yml"

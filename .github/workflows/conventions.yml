name: Enforce conventions

on:
  pull_request:
    types: [opened, labeled, unlabeled, synchronize]

jobs:
  run:
    uses: RubberDuckCrew/.github/.github/workflows/action-conventions.yml@ce18923b2f09dce39930cd8c370ea45c8e58c414 # v2.0.0
    permissions:
      issues: write
      pull-requests: write

name: Actionlint

on:
  push:
    paths: [.github/workflows/**]

jobs:
  run:
    uses: RubberDuckCrew/.github/.github/workflows/action-actionlint.yml@5a0a828ec10bcebf7c980d13fca16f99be69f6d0 # v2.4.0

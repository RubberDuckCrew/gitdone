name: Build and deploy docs

on:
  push:
    branches: [main]
    paths: [docs/**, .github/workflows/docs.yml]
  pull_request:
    types: [opened, labeled, synchronize]
    paths: [docs/**, .github/workflows/docs.yml]

jobs:
  run:
    uses: RubberDuckCrew/.github/.github/workflows/action-docs.yml@ce18923b2f09dce39930cd8c370ea45c8e58c414 # v2.0.0
    secrets: inherit
    with:
      project-name: gitdone
    permissions:
      contents: read
      deployments: write
      pull-requests: write
